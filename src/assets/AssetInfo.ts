interface AssetInfo {
    vertices: number[];
    indices: number[];
    texcoords: number[];
    vertexShaderSource: string;
    fragmentShaderSource: string;
    textureSource: string;
}

export default AssetInfo;
